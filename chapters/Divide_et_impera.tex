\chapter{The \textit{Divide et Impera} Architecture}
\label{chap:divide_et_impera}

This chapter introduces the \textit{Divide et Impera} (Divide and Conquer) approach, our final and most advanced architectural proposal for solving large-scale QUBO problems. Designed to explicitly bypass the computational and hardware bottlenecks identified in the previous chapters, this distributed architecture successfully scales up to massive 100-variable instances. We first outline the general pipeline of this framework, followed by an in-depth algorithmic breakdown of its core components. Finally, we present the experimental results obtained from testing this architecture on a set of dense $100\times100$ matrices, drawing our ultimate conclusions on its performance and overall scalability.

\section{Pipeline Overview: Divide et Impera phase}
\label{sec:divide_et_impera_pipeline_divide}

The fundamental execution flow of our distributed architecture is visually summarized in Figure \ref{fig:divide_et_impera_pipeline}. This conceptual logic was explicitly implemented within our codebase through the \textit{bench-partitioned-GA.py} and \textit{bench-SA-partitioned-greedy-init.py} orchestration scripts \cite{orsucci_qubo_ga_quantum}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{img/divide_et_impera/divide_et_impera_pipeline.png} 
    \caption{Flowchart of the \textit{Divide et Impera} pipeline. The process highlights a distributed hybrid classical-quantum approach, introducing a significantly more sophisticated routing and partitioning logic compared to the baseline pipeline.}
    \label{fig:divide_et_impera_pipeline}
\end{figure}

As illustrated in Figure \ref{fig:divide_et_impera_pipeline}, the conceptual schema shares its foundational roots with the baseline pipeline explored in Chapters 4 and 5, yet it introduces a radical paradigm shift in handling the embedding phase. Rather than forcing a massive, densely connected QUBO matrix onto a single constrained spatial register, the original problem is algorithmically shattered into smaller, geometrically manageable sub-problems. These disjointed sub-graphs are then individually embedded and routed to the analog quantum emulator for independent execution. Once all the distributed quantum jobs are completed (as detailed later in this chapter), their localized analog readouts are computationally stitched back together to reconstruct the global state of the original combinatorial problem.

This localized partitioning strategy allows us to solve massive QUBO instances that would otherwise be strictly infeasible due to geometric constraints or excessive qubit requirements. By breaking the problem down, we effectively bypass the physical limitations of the Neutral Atom QPU as well as the exponential execution delays that plague classical preprocessing heuristics on large graph spaces.
Furthermore, this modular approach possesses inherent scalability. As quantum hardware capacities and classical embedding heuristics continue to evolve, the architecture can straightforwardly accommodate larger baseline partitions, thereby seamlessly increasing the maximum size of the global instances that can be targeted. 

The following sections detail the novel algorithmic components that make this distributed architecture possible.

\subsection{Spectral Clustering and Graph Translation}
\label{subsec:spectral_clustering}

The most critical phase of this architecture is the intelligent topological division of the target problem. Randomly splitting the QUBO matrix would sever crucial quadratic interactions, devastating the physical meaning of the final solution. Therefore, the problem is structurally translated into an equivalent network topology by treating the QUBO matrix $Q$ as a weighted adjacency matrix.

Specifically, we extract an undirected Affinity Matrix $A$ representing the structural dependencies of the system. The affinity between any two nodes $i$ and $j$ is defined by the absolute magnitude of their quadratic interaction, while explicitly ignoring the linear local detuning terms on the main diagonal:
\begin{equation}
    A_{ij} = |Q_{ij}| (1 - \delta_{ij})
\end{equation}
where $\delta_{ij}$ is the Kronecker delta (which equals $1$ when $i=j$, and $0$ otherwise). This mathematical formulation serves a fundamental algorithmic purpose: it forces all diagonal elements to zero ($A_{ii} = 0$). In a QUBO matrix, the diagonal values represent linear bias terms (individual node weights), whereas the off-diagonal values represent the quadratic couplings (the actual structural edges connecting different nodes). Since Spectral Clustering relies on the strength of network connections to identify optimal graph cuts, including individual node biases would heavily skew the partitioning logic. By leveraging the Kronecker delta, we mathematically guarantee that the clustering algorithm focuses exclusively on the pure topological interconnectivity of the network.

Furthermore, the absolute value is employed because, for the physical embedding constraints, any strong interaction (whether strictly attractive or strictly repulsive) dictates that the respective neutral atoms must be placed in close spatial proximity to successfully execute the Rydberg blockade dynamics.

With the topological affinity mapped, the pipeline employs \textbf{Spectral Clustering} to partition the network. The mathematical foundation of this technique relies on the construction of the Graph Laplacian. First, we define the Degree Matrix $D$, a diagonal matrix where each entry represents the sum of the affinities attached to a specific node:
\begin{equation}
    D_{ii} = \sum_{j} A_{ij}
\end{equation}
Subsequently, the unnormalized Graph Laplacian matrix $L$ is computed as the difference between the Degree Matrix and the Affinity Matrix:
\begin{equation}
    L = D - A
\end{equation}

Spectral clustering evaluates the eigenvalues and eigenvectors of this Laplacian matrix $L$. Specifically, the multiplicity of the zero eigenvalue indicates the number of disconnected components in the graph, while the eigenvector associated with the second smallest eigenvalue (known as the Fiedler vector \cite{spectral_MA}) provides the optimal continuous relaxation for the discrete graph partitioning problem. By mapping the nodes into this lower-dimensional spectral space, the algorithm identifies natural topological "cuts" within the network, effectively grouping nodes that share dense interconnections while severing the weakest links.

However, standard spectral clustering algorithms require a predefined number of target clusters ($k$), and they do not inherently guarantee an upper limit on the size of the resulting partitions. To strictly enforce our hardware limitations, we implemented a \textbf{Recursive Size-Capped} logic. The pipeline dynamically calculates the number of expected sub-clusters as:
\begin{equation}
    k = \max(2, \text{round}(N / \text{target\_capacity}))
\end{equation}
and tolerates only a strictly defined variance. Once the spectral cut is executed, the algorithm evaluates the cardinality of each resulting sub-graph. If any partition still exceeds the maximum hardware capacity, defined as:
\begin{equation}
    \text{target\_size} + \text{tolerance}
\end{equation}
the spectral clustering algorithm recursively calls itself exclusively on that overgrown sub-graph. This procedural recursion is visually represented in the looping feedback path of Figure \ref{fig:divide_et_impera_pipeline}. 

To formally wrap-up a single iteration of this spatial division, we present the algorithmic pseudo-code derived from \cite{spectral_DM}:

\begin{figure}[H]
    \centering
    \fbox{
    \begin{minipage}{0.85\textwidth}
    \vspace{0.2cm}
    \textbf{Algorithm: Spectral Graph Clustering}
    \begin{enumerate}
        \item Create a sparsified similarity graph $\mathcal{G}$ (represented by our matrix $A$).
        \item Compute the graph Laplacian for $\mathcal{G}$, denoted as $\mathbf{L}$.
        \item Create a matrix $\mathbf{V}$ consisting of the first $k$ eigenvectors of $\mathbf{L}$.
        \item Apply K-means clustering on the rows of $\mathbf{V}$ to obtain the $k$ discrete clusters.
    \end{enumerate}
    \vspace{0.1cm}
    \end{minipage}
    }
    \caption{High-level algorithmic description of a single Spectral Clustering iteration, adapted from \cite{spectral_DM}.}
    \label{fig:spectral_algorithm}
\end{figure}

Ultimately, this highlights how the graph translation remains entirely implicit: no supplementary data structures are instantiated, completely avoiding unnecessary computational overhead. Furthermore, this recursive process guarantees that every single combinatorial fragment produced by the orchestrator strictly complies with the maximum qubit capacity of the analog QPU, eliminating the risk of un-embeddable graphs.

\subsection{Dynamic Heuristic Routing}
\label{subsec:dynamic_heuristic_routing}

As shown in Figure \ref{fig:divide_et_impera_pipeline}, once the global problem is partitioned into smaller sub-problems, each fragment is independently routed through a dynamic switch mechanism. Acknowledging that deploying complex metaheuristics and analog quantum emulation for trivial problems is computationally wasteful, the pipeline evaluates the size of each sub-cluster against a \texttt{min\_size} threshold (configured between 3 and 5 nodes in our parametrization). Based on this threshold, the architecture automatically selects one of two distinct evaluation strategies:

\begin{itemize}
    \item \textbf{Exact Classical Solver (Brute-Force):} If the sub-graph cardinality is strictly smaller than the \texttt{min\_size} threshold, the spatial embedding phase and the quantum QPU execution are entirely bypassed. The localized combinatorial fragment is so trivial that it is instantly and optimally solved using an exact classical brute-force evaluation over its discrete binary space ($x \in \{0,1\}^n$).
    \item \textbf{Heuristic Embedding (GA/SA):} For larger sub-graphs that exceed the trivial threshold, the pipeline routes the matrix to the core embedding engines. Here, either the Genetic Algorithm (GA) or the Simulated Annealing (SA) heuristic is employed to navigate the continuous spatial search space, mapping the logical nodes to physical atom coordinates. This balances computational efficiency with embedding success rates, successfully preparing complex localized layouts for the analog quantum execution.
\end{itemize}

Crucially, this dynamic routing logic makes our proposed architecture fundamentally modular and highly extensible, because the \textit{Divide et Impera} framework is completely agnostic to both the specific partitioning method and the underlying embedding algorithms. Consequently, any of these specific components can be seamlessly swapped or upgraded if a more efficient spatial metaheuristic or clustering algorithm is developed in the future.
\section{Pipeline Overview: Retrieval and Merging Phase}
\label{sec:divide_et_impera_pipeline_merging}

The final architectural phase of the \textit{Divide et Impera} pipeline is the retrieval and global merging process. Once the problem has been partitioned and the sub-graphs independently embedded and executed, the localized solutions must be meticulously reconstructed to form a valid, high-quality global solution for the original QUBO instance. 

The execution logic of this reconstruction phase is visually summarized in Figure \ref{fig:retrieval_merging_pipeline}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{img/divide_et_impera/Retrieval-merging.png} 
    \caption{Flowchart of the Retrieval and Merging phase. The diagram illustrates the asynchronous data retrieval, the exact energy-based filtering for quantum results, the deterministic greedy conflict resolution, and the final thermodynamic fine-tuning.}
    \label{fig:retrieval_merging_pipeline}
\end{figure}

As implemented in our orchestration scripts, this phase strictly avoids naive concatenations, which would inevitably trigger massive physical and mathematical conflicts at the boundaries of the disjointed clusters. Instead, the process is divided into three highly specialized sequential steps:

\begin{enumerate}
    \item \textbf{Asynchronous Retrieval and Energy Filtering:} 
    Relying on a previously compiled JSON job registry, the orchestrator queries the localized results. If a sub-problem was historically classified as trivial (falling below the \texttt{min\_size} threshold), its exact classical brute-force solution is instantly retrieved. Conversely, for jobs submitted to the analog quantum emulator, the system retrieves a statistical distribution of bitstrings. 
    Crucially, instead of blindly selecting the most frequent quantum measurement, the pipeline evaluates every unique sampled state against its specific local QUBO sub-matrix in order to find the true local minimum:
    \begin{equation}
        E_{local} = x^T Q_{sub} x
    \end{equation}
    The bitstring that yields the absolute minimum local energy is selected. This computational filtering acts as a powerful error-mitigation strategy, effectively bypassing potential quantum noise or false local minima that might dominate the raw probability distribution.

    \item \textbf{Greedy Conflict Resolution:} 
    Once the best local bitstrings are identified, they must be combined. Because the independent clusters might propose conflicting Rydberg excitations (setting adjacent boundary nodes to $1$), a deterministic greedy merging protocol is enforced, inspired by the methodologies discussed in \cite{das2025grid}. The algorithm operates as follows:
    \begin{itemize}
        \item It isolates all nodes proposed to be in the excited state (bit = $1$) across all local solutions.
        \item It sorts these candidate nodes based on their linear diagonal weights ($Q_{ii}$), prioritizing the variables with the strongest intrinsic bias.
        \item It iterates through the sorted list, accepting a node into the final global state \textit{only} if its inclusion does not generate a mathematical conflict (i.e., $Q_{ij} > 0$) with any previously accepted node.
    \end{itemize}
    This fast, deterministic approach guarantees that the reconstructed global solution strictly adheres to the topological constraints of the problem, resolving boundary overlaps efficiently.

    \item \textbf{Low-Temperature SA Tuning (Post-Processing):} 
    While the greedy approach successfully generates a valid global state, it is inherently deterministic and prone to settling into sub-optimal configurations at the cluster intersections, exposing its limitations as also highlighted in \cite{das2025grid}. To address this, our proposed improvement applies a final localized perturbation using Simulated Annealing. 
    Unlike a standard SA execution starting from a random state, this phase utilizes the greedy-merged bitstring as a \textit{hot-start} initialization. By restricting the inverse temperature parameter ($\beta$) to a narrow, high-value range, the thermodynamic engine performs strictly localized, low-temperature micro-adjustments. This fine-tuning squeezes out the remaining topological errors without destroying the highly optimized global structure assembled in the previous steps.
\end{enumerate}

Ultimately, this multi-tiered reconstruction ensures that the hybrid architecture extracts the maximum possible value from the localized quantum executions, delivering a highly competitive global energy minimum.

\section{Experimental Results}
\label{sec:divide_et_impera_results}

This section presents the most significant and representative experimental results obtained using the \textit{Divide et Impera} pipeline. For the sake of brevity, and because the core topological and computational behaviors remain consistent, this analysis will focus exclusively on the Simulated Annealing (SA) variant of the distributed architecture. While the Genetic Algorithm (GA) approach proved to be functionally effective, it is omitted from this detailed exposition due to its inherent limitations regarding the maximum manageable sub-problem capacity and overall execution time. The SA engine, conversely, allows us to push the true boundaries of the architecture, effectively targeting massive global instances by accommodating significantly larger individual clusters.

Specifically, the experimental validation targets massive problem scales, notably $50\times50$ and $100\times100$ QUBO matrices. Due to their overwhelming geometric density and dimensionality, these specific instances cannot be visually depicted as was done for smaller topologies. Instead, they will be analytically characterized strictly through their core topological and statistical metrics.

\subsection{Scaling to 50 Nodes: A $50\times50$ Instance}
\label{subsec:divide_et_impera_50x50}

The first large-scale validation of the \textit{Divide et Impera} architecture is performed on a $50\times50$ QUBO matrix. At this dimensionality, the continuous spatial search space completely exceeds the capabilities of the baseline monolithic embedding approaches discussed in the previous chapters. To properly contextualize the computational challenge, the topological complexity of this specific instance is analytically characterized by the following structural metrics:

\begin{itemize}
    \item \textbf{Graph Density:} $10.9\%$ ($134$ active connections out of $1225$ possible undirected edges).
    \item \textbf{Node Degree Distribution:} Minimum degree of $2$, maximum degree of $9$, with an average connectivity of $5.4$ edges per node.
    \item \textbf{Weight Uniformity:} Constant off-diagonal penalty of $2.00$ and a uniform linear diagonal mean of $1.00$ (Standard Deviation of edges: $0.0$). This perfectly adheres to the physical constraints enforced by the QPU global laser driving mechanism and the generation constraints as for the previous instances.
\end{itemize}

While an overall density of $10.9\%$ might appear mathematically sparse, in the context of 2D continuous spatial embeddings, it still presents a formidable geometric challenge. Specifically, a maximum node degree of $9$ dictates that at least one atomic qubit must be physically surrounded by $9$ distinct, highly-interacting neighbors within its Rydberg blockade radius ($R_b$). Balancing this dense local clustering while strictly avoiding physical overlap violations ($d_{min}$) requires a highly optimized topological layout. Consequently, this instance serves as a rigorous initial stress test for the clustering and distributed partitioning logic of our architecture.

As governed by the orchestrator, the Spectral Clustering engine successfully shattered the $50$-node global problem into $5$ independent, geometrically manageable sub-clusters (containing $11$, $8$, $11$, $11$, and $9$ nodes, respectively). Because the dimensionality of each sub-problem is vastly reduced compared to a monolithic approach, the Simulated Annealing engine no longer requires the extreme thermodynamic brute-force configuration previously seen for the $20\times20$ matrix. Instead, a much lighter, adaptive parametrization was dynamically deployed based on the cluster size, as detailed in Table \ref{tab:sa_params_50x50}:

\begin{table}[H]
    \centering
    \resizebox{1.0\textwidth}{!}{
    \begin{tabular}{|l|c|c|p{7cm}|}
    \hline
    \textbf{SA Parameter} & \textbf{Standard Cluster} (9-11 nodes) & \textbf{Small Cluster} (8 nodes) & \textbf{Conceptual Meaning} \\
    \hline
    \texttt{num\_restarts} & 5 & 5 & Reduced multi-start execution (down from 15). The smaller spatial search space mitigates the need for aggressive stochastic restarts. \\
    \hline
    \texttt{T\_INIT} & 100.0 & 80.0 & Initial Temperature. Kept relatively low to preserve the structural integrity of the GRASP pre-placer initialization. \\
    \hline
    \texttt{T\_MIN} & 0.01 & 0.10 & Minimum Temperature stopping criterion before the final Quenching phase. \\
    \hline
    \texttt{COOLING\_RATE} & 0.98 & 0.95 & Exponential decay factor. Faster cooling is permitted on smaller topologies. \\
    \hline
    \texttt{STEPS\_PER\_TEMP} & 1000 & 300 & MCMC steps per epoch. Drastically reduced compared to monolithic executions, significantly cutting down classical preprocessing time. \\
    \hline
    \end{tabular}
    }
    \caption{Adaptive thermodynamic hyperparameter configuration deployed for the fragmented $50\times50$ QUBO instance.}
    \label{tab:sa_params_50x50}
\end{table}

This adaptive configuration definitively proves the primary computational advantage of the \textit{Divide et Impera} paradigm: by distributing the topological complexity, the localized classical embedding times drop drastically. The architecture effectively transforms a previously intractable $50$-node continuous mapping problem into a series of highly efficient, computationally tractable geometric optimization tasks.

However, while the classical execution time drops considerably thanks to smaller clusters, the primary focus of this evaluation remains the convergence quality of the final combinatorial state and the overall resilience of the architecture. As observed during the retrieval phase, the pipeline actively mitigates quantum noise by evaluating the exact local energy of every unique bitstring sampled by the QPU, rather than blindly trusting the highest-probability measurement. Upon executing the deterministic greedy merging protocol on these filtered local minima, the reconstructed global solution yielded a highly competitive baseline energy of $-14.0$. 

Crucially, the architecture demonstrates an outstanding algorithmic resilience against suboptimal spatial embeddings. Despite any residual topological penalties accumulated within the individual fragmented clusters, the combination of the exact energy-based quantum state filtering and the subsequent low-temperature SA tuning successfully absorbed these imperfections. This dual-layered mitigation seamlessly refined the boundary interactions between the disjointed graphs, dropping the final global energy to $-16.0$. This refined result perfectly matches the global minimum independently approximated using the D-Wave Simulated Annealing classical benchmark.

Consequently, this provides compelling evidence that the \textit{Divide et Impera} pipeline is not only highly effective at scaling to larger dimensions but is also robust. It maintains the capability to reliably identify global optima even when the underlying mathematical problem is heavily fragmented and the isolated physical embeddings are geometrically imperfect.


\subsection{The Operational Frontier: A $100\times100$ Instance}
\label{subsec:divide_et_impera_100x100}

Continuing the scalability evaluation, the architecture is pushed to a new experimental frontier by targeting a $100\times100$ QUBO matrix. Embedding an instance comprised of $100$ fully interactive variables represents the operational frontier of this research context. As with the previous large-scale matrix, visually plotting the target graph or the resulting physical register layout is practically unfeasible due to the overwhelming scale. Consequently, the topological complexity of this ultimate stress test is defined through its structural metrics:

\begin{itemize}
    \item \textbf{Graph Density:} $4.8\%$ ($238$ active connections out of $4950$ possible undirected edges).
    \item \textbf{Node Degree Distribution:} Minimum degree of $1$, maximum degree of $10$, with an average connectivity of $4.8$ edges per node.
    \item \textbf{Weight Uniformity:} Constant off-diagonal penalty of $2.00$ and a uniform linear diagonal mean of $1.00$ (Standard Deviation of edges: $0.0$). This perfectly adheres to the physical constraints enforced by the QPU global laser driving mechanism and the generation constraints as for the previous instances.
\end{itemize}

While the overall graph density dramatically drops to $4.8\%$ (compared to the $10.9\%$ of the $50\times50$ matrix), this statistical dilution is entirely deceptive. The true geometric bottleneck is exposed by the maximum node degree of $10$. In physical terms, this mandates that a single atomic qubit must be tightly surrounded by $10$ distinct, highly-interacting neighbors, all packed strictly within the limited Rydberg blockade radius ($R_b$) while rigorously maintaining the minimum optical trap separation ($d_{min}$). Satisfying this extreme local density on a 2D continuous plane constitutes a substantial embedding challenge, making this instance a definitive stress test for the \textit{Divide et Impera} architecture.

As governed by the orchestrator, the Spectral Clustering engine successfully divided the massive $100$-node global problem into $10$ independent sub-clusters. Their dimensionalities exhibited a wider variance compared to previous tests, yielding sizes of $15$, $14$, $13$, $11$, $10$, $10$, $9$, $8$, $7$, and a trivial $2$-node pair. In strict accordance with the dynamic routing logic introduced in Section \ref{subsec:dynamic_heuristic_routing}, the $2$-node component was intercepted and instantly resolved via the Exact Classical Solver, completely bypassing the spatial embedding overhead. 

For the remaining $9$ clusters routed to the QPU emulator, the Simulated Annealing engine deployed a highly granular, three-tiered adaptive parametrization based on cluster size, as detailed in Table \ref{tab:sa_params_100x100}:

\begin{table}[H]
    \centering
    \resizebox{1.0\textwidth}{!}{
    \begin{tabular}{|l|c|c|c|p{5cm}|}
    \hline
    \textbf{SA Parameter} & \textbf{Large Cluster} (13-15 nodes) & \textbf{Standard} (9-11 nodes) & \textbf{Small} (7-8 nodes) & \textbf{Conceptual Meaning} \\
    \hline
    \texttt{num\_restarts} & 5 & 5 & 5 & Consistent multi-start execution across all routed clusters. \\
    \hline
    \texttt{T\_INIT} & 1000.0 & 100.0 & 80.0 & Higher initial temperature reserved exclusively for the largest topologies to safely overcome denser initial constraints. \\
    \hline
    \texttt{T\_MIN} & 0.01 & 0.01 & 0.10 & Minimum Temperature stopping criterion before the Quenching phase. \\
    \hline
    \texttt{COOLING\_RATE} & 0.99 & 0.98 & 0.95 & Extremely slow exponential decay (0.99) utilized to stabilize the highly-dense 13-15 node clusters. \\
    \hline
    \texttt{STEPS\_PER\_TEMP} & 1200 & 1000 & 300 & MCMC steps per epoch. Scaled proportionally to the expanding geometric continuous search space. \\
    \hline
    \end{tabular}
    }
    \caption{Three-tiered adaptive thermodynamic hyperparameter configuration deployed for the fragmented $100\times100$ QUBO instance.}
    \label{tab:sa_params_100x100}
\end{table}

This granular routing and parametrization strategy definitively proves that distributing the topological complexity maintains the classical direct embedding difficulty well within computationally tractable limits, avoiding the exponential wall and physical hardware limitations that paralyze monolithic approaches.

However, the evaluation of this $100$-node instance solidifies the critical importance of the algorithmic resilience designed into the retrieval phase. Experimental logs reveal a stark contrast in performance based on the specific data extraction strategy. When the pipeline blindly accepted the most probable quantum measurement for each cluster (the standard baseline approach), the greedy merging process yielded an initial global energy of $-29.0$. Conversely, by actively mitigating quantum noise through the exact local energy evaluation of every unique bitstring sampled by the QPU, the baseline greedy merging score surged to a remarkable $-35.0$.

This massive pre-tuning advantage provides the ultimate demonstration of the architecture's robustness. Despite the residual topological penalties accumulated within the most geometrically complex clusters, feeding this highly optimized, noise-filtered state into the low-temperature SA post-processing engine flawlessly refined the boundary interactions. The global energy successfully converged to $-37.0$. This refined result perfectly matches the true global minimum independently approximated by the D-Wave Simulated Annealing classical benchmark.

Ultimately, this $100\times100$ stress test definitively validates the \textit{Divide et Impera} architecture. It proves that by intelligently distributing spatial complexity, deploying adaptive classical heuristics, and systematically mitigating quantum readout noise, the hybrid pipeline can reliably solve massive, physically constrained combinatorial problems without sacrificing optimality. Moving forward, a compelling open question remains: to what extent can this pipeline maintain its resilience when confronted with even denser and larger instances?
\subsection{Pushing the Density Limit: A Denser $100\times100$ Instance}
\label{subsec:divide_et_impera_100x100_dense}

To answer the concluding question of the previous section and establish the absolute structural limits of the \textit{Divide et Impera} architecture, a final experimental stress test is conducted. While the global dimensionality remains fixed at $N=100$ nodes, this specific target matrix is engineered to exhibit a significantly higher topological density. The structural metrics defining this ultimate combinatorial challenge are outlined as follows:

\begin{itemize}
    \item \textbf{Graph Density:} $6.6\%$ ($325$ active connections out of $4950$ possible undirected edges).
    \item \textbf{Node Degree Distribution:} Minimum degree of $1$, maximum degree of $13$, with an average connectivity of $6.5$ edges per node.
    \item \textbf{Weight Uniformity:} Constant off-diagonal penalty of $2.00$ and a uniform linear diagonal mean of $1.00$ (Standard Deviation of edges: $0.0$).
\end{itemize}

While a jump from $4.8\%$ to $6.6\%$ global density might appear numerically modest, the true severity of this instance lies in its maximum node degree of $13$. Translating this metric into the physical domain of Neutral Atom arrays implies that a single atomic qubit must successfully accommodate $13$ distinct, interacting neighbors within its identical Rydberg blockade sphere ($R_b$). In a 2D continuous space heavily restricted by minimum atomic separation boundaries ($d_{min}$), achieving such localized geometric saturation without violating overlap constraints borders on physical intractability. Consequently, this instance serves as the definitive evaluation of the pipeline's algorithmic resilience under extreme geometric pressure.

As the dense $100$-node graph undergoes the topological partitioning phase, the impact of the increased geometric saturation becomes immediately apparent. The Spectral Clustering engine divided the problem into $9$ independent sub-clusters, yielding dimensionalities of $17$, $16$, $14$, $12$, $11$, $9$, $9$, $7$, and $5$ nodes. Unlike the sparser matrices analyzed previously, the high local entanglement prevented the creation of trivial sub-graphs: every single fragment exceeded the threshold for exact classical solvers, forcing all computations to the heuristic and quantum emulators. Most notably, the algorithm was forced to isolate massive $16$- and $17$-node clusters to successfully preserve the critical, highly-packed interaction weights without severing the problem's underlying physics.

To accommodate this severe geometric saturation without causing a classical computational bottleneck or a catastrophic overlap collision, the orchestrator dynamically expanded the adaptive Simulated Annealing parametrization into a highly granular four-tiered system. As detailed in Table \ref{tab:sa_params_100x100_dense}, an "Extreme" parameter tier was exclusively introduced to handle the $17$-node fragment:

\begin{table}[H]
    \centering
    \resizebox{1.0\textwidth}{!}{
    \begin{tabular}{|l|c|c|c|c|p{4cm}|}
    \hline
    \textbf{SA Parameter} & \textbf{Extreme} (17 nodes) & \textbf{Large} (14-16) & \textbf{Standard} (9-12) & \textbf{Small} (5-7) & \textbf{Conceptual Meaning} \\
    \hline
    \texttt{num\_restarts} & 5 & 5 & 5 & 5 & Consistent multi-start execution across all clusters. \\
    \hline
    \texttt{T\_INIT} & 2000.0 & 1000.0 & 100.0 & 80.0 & Massive initial temperature surge strictly required for the 17-node cluster to escape deep geometric local minima. \\
    \hline
    \texttt{T\_MIN} & 0.01 & 0.01 & 0.01 & 0.10 & Minimum Temperature stopping criterion before the Quenching phase. \\
    \hline
    \texttt{COOLING\_RATE} & 0.995 & 0.990 & 0.980 & 0.950 & Exceptionally slow cooling (0.995) deployed to stabilize the highly-dense extreme cluster without atomic collisions. \\
    \hline
    \texttt{STEPS\_PER\_TEMP} & 2000 & 1200 & 1000 & 300 & MCMC steps per epoch dynamically scaled to the exponentially expanding continuous search space. \\
    \hline
    \end{tabular}
    }
    \caption{Four-tiered adaptive thermodynamic hyperparameter configuration deployed for the dense $100\times100$ QUBO instance.}
    \label{tab:sa_params_100x100_dense}
\end{table}

Despite the staggering topological difficulty of the individual fragmented components, the final evaluation phase confirms the absolute structural resilience of the architecture. The pipeline successfully filtered the quantum measurements to identify the exact local energy minima across all 9 clusters. Applying the deterministic greedy merging protocol yielded a highly robust baseline global energy of $-26.0$. 

Given the dense boundary intersections between the highly entangled sub-graphs, residual topological conflicts and energy penalties were inevitable. However, the \textit{hot-start} low-temperature SA tuning effortlessly absorbed these boundary imperfections. The post-processing engine elegantly refined the global state intersections, successfully dropping the cumulative combinatorial cost to an optimal $-30.0$. This final energy score perfectly matches the true mathematical limit independently computed via the monolithic D-Wave Simulated Annealing benchmark.

Ultimately, this extreme experimental configuration serves as the definitive culmination of our research. It provides a good evidence that the \textit{Divide et Impera} architecture, supported by intelligent data routing and dynamically scaling thermodynamic heuristics, is structurally robust. It effectively mitigates quantum readout noise and navigates heavily saturated topologies, guaranteeing the identification of global optimal solutions even under the most severe hardware constraints imposed by the Neutral Atom platform.

